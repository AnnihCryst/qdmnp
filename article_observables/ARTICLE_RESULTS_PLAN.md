# Минимальный план расчётов и каркас раздела результатов статьи

## Назначение документа

Этот документ задаёт не просто порядок запуска программ, а причинно связанную
последовательность физических вопросов, вычислений, рисунков и выводов будущей
статьи. План построен для композитной системы из коллоидной квантовой точки
(КТ) и одной золотой осесимметричной металлической наночастицы (МНЧ),
представленной реализованным в проекте вытянутым сфероидом с полуосями
\(c>a\). КТ рассматривается как точечный диполь.

Цель минимального набора — получить ровно те результаты, которые нужны, чтобы
последовательно ответить на вопросы:

1. усиливает или подавляет МНЧ резонансное возбуждение экситона;
2. уменьшает ли МНЧ требуемый флюенс и, при фиксированном импульсе, пиковую
   интенсивность;
3. как меняются положение и ширина резонансной области;
4. на каких расстояниях точечно-дипольная модель МНЧ (DD) согласуется с более
   полной пространственной моделью (FQS);
5. где следует расположить КТ и как ориентировать электрическое поле;
6. насколько количественные результаты зависят от однополюсного или
   многополюсного описания дисперсии золота;
7. как связать рассчитанную динамику КТ с Shah-подобным спектральным
   оптическим показателем.

Каждый calculator сохраняет самодостаточный NPZ: фактически использованные
входы, физические константы, исходные табличные данные материала, параметры
причинной аппроксимации, рассчитанные массивы, единицы и диагностические
сертификаты. Соответствующий plotter получает только этот NPZ и не запускает
тяжёлое вычисление заново.

Старая программа `qd_mnp_legacy_inverse_alpha_absorption.py` в этот план не
входит и не должна использоваться для данных статьи.

## 1. Итоговый минимальный набор

Рекомендуемое ядро состоит из **шести нумерованных рисунков статьи**. Для них
используются девять различных пар `calculator -> NPZ -> plotter`. Пара
`model_discrepancy_gap` применяется дважды — к спектральному и пороговому
master-NPZ, — поэтому фактически получается десять запусков пар. Четыре
производных запуска только извлекают метрики из уже полученных NPZ и не решают
физическую модель повторно.

| Рисунок статьи | Вычисляемая зависимость | Пары программ | Главный вопрос |
|---|---|---|---|
| 1 | \(\alpha_{\parallel,\perp}(E)\), \(\alpha^{-1}_{\parallel,\perp}(E)\): `direct`, \(N=1\), \(N=N_{\rm multi}\) | `qd_mnp_calculate_material_dispersion_comparison.py` -> `qd_mnp_plot_material_dispersion_comparison.py` | Действительно ли многополюсная реализация лучше описывает выбранную табличную дисперсию золота? |
| 2a | \(G_{\rm exc}(g)\): DD и FQS | `qd_mnp_calculate_excitation_gain_gap.py` -> `qd_mnp_plot_excitation_gain_gap.py` | Усиливает или подавляет МНЧ слабополевой резонансный отклик КТ? |
| 2b | \(\Delta E_{\rm res}(g)/\Gamma_0\): DD и FQS | `qd_mnp_calculate_resonance_shift_gap.py` -> `qd_mnp_plot_resonance_shift_gap.py` | Насколько нужно перестраивать частоту лазера? |
| 2c | \(\Gamma_{\rm eff}(g)/\Gamma_0\): DD и FQS | `qd_mnp_calculate_spectral_width_gap.py` -> `qd_mnp_plot_spectral_width_gap.py` | Как изменяется рабочая ширина экситонного резонанса? |
| 2d | \(\delta_{\rm spec}(g)\): DD относительно FQS | `qd_mnp_calculate_model_discrepancy_gap.py` -> `qd_mnp_plot_model_discrepancy_gap.py` | С какого зазора DD воспроизводит FQS с заранее выбранной точностью? |
| 3a | \(\mathcal F_\eta(g)/\mathcal F_{\eta,0}\): DD и FQS | `qd_mnp_calculate_threshold_fluence_gap.py` -> `qd_mnp_plot_threshold_fluence_gap.py` | Во сколько раз меняется требуемый флюенс короткого импульса и какая конфигурация практически предпочтительна? |
| 3b | \(\delta_{\mathcal F}(g)\): DD относительно FQS | `qd_mnp_calculate_model_discrepancy_gap.py` -> `qd_mnp_plot_model_discrepancy_gap.py`, повторно для порогового NPZ | С какого зазора DD воспроизводит именно нелинейный короткоимпульсный порог FQS? |
| 4 | \(S(E)\): `direct`, \(N=1\), \(N=N_{\rm multi}\), FQS | `qd_mnp_calculate_excitation_spectrum_material_comparison.py` -> `qd_mnp_plot_excitation_spectrum_material_comparison.py` | Как ошибка материальной дисперсии переносится в положение, ширину и амплитуду КТ-селективного резонанса? |
| 5 | \(P_{\rm exc}(\mathcal F)\): изолированная КТ, \(N=1\), \(N=N_{\rm multi}\), FQS | `qd_mnp_calculate_excitation_fluence_material_comparison.py` -> `qd_mnp_plot_excitation_fluence_material_comparison.py` | Как дисперсионная модель меняет нелинейное возбуждение КТ коротким импульсом и порог? |
| 6 | \(\sigma_{\rm QS,work}(E;\mathcal F_j)\) и \(\Delta\sigma(E;\mathcal F_j)\): \(N=1\), \(N=N_{\rm multi}\), FQS | `qd_mnp_calculate_work_loss_spectrum_material_comparison.py` -> `qd_mnp_plot_work_loss_spectrum_material_comparison.py` | Проявляется ли изменение возбуждения КТ в Shah-подобном оптическом спектре гибрида? |

Таким образом, получается десять файлов изображений. Четыре изображения
`fig02a`--`fig02d` при вёрстке объединяются в один четырёхпанельный рисунок 2,
а `fig03a`--`fig03b` — в один двухпанельный рисунок 3. Остальные четыре
изображения являются самостоятельными рисунками 1 и 4--6.

С точки зрения стоимости это шесть независимых master-расчётов: три
частотных/fit-блока (рисунки 1, 2 и 4) и три тяжёлых временных блока (рисунки
3, 5 и 6). Ещё четыре calculator-запуска — 2b, 2c, 2d и 3b — являются только
постобработкой уже сохранённых массивов. Все plotter-ы читают NPZ и не вызывают
математические ядра, поэтому стиль каждого изображения можно менять без
повторного физического расчёта.

Это минимальный **защищаемый**, а не формально самый короткий набор. Удаление
любой основной части оставляет без прямого ответа один из поставленных
руководителем вопросов:

- без 2a нельзя количественно говорить об усилении или подавлении;
- без 2b нельзя говорить о сдвиге;
- без 2c нельзя говорить об уширении;
- без 2d нельзя указать расстояние применимости DD;
- без 3a нельзя обосновать рекомендацию по требуемой интенсивности, а без 3b
  нельзя перенести границу DD/FQS на центральную нелинейную метрику статьи;
- без 1 и 4 нельзя показать, что \(N_{\rm multi}\) не просто отличается от
  \(N=1\), а лучше воспроизводит частотный эталон внутри принятой модели;
- без 5 не показано нелинейное короткоимпульсное следствие этой разницы;
- без 6 нет требуемой связи с оптическими спектрами работы Shah.

## 2. Две независимые оси сравнения

В статье необходимо строго разделить два разных уточнения модели.

| Блок сравнения | Что изменяется | Что обязательно фиксируется | Допустимый вывод |
|---|---|---|---|
| Материальная дисперсия, рисунки 1, 4--6 | Число материальных лоренцевых полюсов: \(N=1\) и \(N=N_{\rm multi}\); в частотных расчётах присутствует `direct` | FQS-пространственный кернел, геометрия, положение КТ, направление \(\mathbf E\), спектральные и импульсные параметры | Влияние точности частотного представления золота на результат |
| Пространственная модель, рисунки 2--3 | DD против FQS | Один и тот же материал, КТ, импульс, зазор, направление \(\mathbf E\), сетки и момент чтения | Ошибка точечно-дипольного пространственного приближения МНЧ |

Материальная аппроксимация имеет вид

\[
\alpha_N(\omega)=\alpha_\infty+
\sum_{s=1}^{N}
\frac{f_s}{\omega_s^2-\omega^2-i\gamma_s\omega}.
\]

Индекс \(s\) нумерует **полюса частотного отклика материала**. Он не нумерует
пространственные сфероидальные гармоники. Пространственный порядок FQS
задаётся независимо параметром `spatial_order_max` и должен быть одинаков для
ветвей \(N=1\) и \(N=N_{\rm multi}\).

Также нельзя автоматически отождествлять проектную ветвь \(N=1\) с
эффективным ярким осциллятором Shah. В этом проекте \(N=1\) — намеренно грубая
однополюсная аппроксимация **той же** табличной дисперсии, относительно которой
строится и многополюсная ветвь.

Полного факторного расчёта

\[
\{\mathrm{DD},\mathrm{FQS}\}\times\{N=1,N=N_{\rm multi}\}
\]

в текущем наборе программ нет. Поэтому план позволяет независимо оценить
материальную и пространственную поправки, но не позволяет без дополнительного
расчёта утверждать что-либо об их нелинейном перекрёстном члене.

## 3. Физическая область применимости и корректные термины

В тексте статьи следует использовать следующие определения.

- **DD** — модель, в которой пространственный отклик МНЧ заменён полем её
  индуцированного точечного диполя.
- **FQS** — полное аналитическое локально-квазистатическое поле однородного
  вытянутого сфероида без точечно-дипольного усечения пространственного
  отклика МНЧ. Слово «точное» допустимо только с этой оговоркой.
- В обеих моделях КТ остаётся точечным диполем.
- \(g\) — зазор поверхность--поверхность, а не расстояние между центрами:

  \[
  R_{\rm tip}=c+r_{\rm QD}+g,
  \qquad
  R_{\rm side}=a+r_{\rm QD}+g.
  \]

- Направление распространения света и волновой вектор в постановку не
  вводятся. Сравнивается только направление электрического поля \(\mathbf E\).
- Совпадение DD и FQS означает согласие двух моделей внутри общей локальной
  квазистатической постановки. Оно не означает согласие с экспериментом или с
  полным решением уравнений Максвелла.

До численного сканирования также должно быть проверено квазистатическое
условие малости характерного размера МНЧ относительно длины волны в среде.
При заметной retardation-поправке сравнивать DD следует уже с ретардированной
электродинамической моделью, а не объявлять локальную FQS количественным
эталоном.

Точка \(g=0\) не должна использоваться как модель «прилипания». Для статьи
нужно задать малый, но положительный и физически обоснованный минимальный
зазор. При субнанометровых зазорах локальный континуальный отклик, точечность
КТ и отсутствие туннелирования, переноса заряда и нелокальности уже не
гарантированы. Такие точки можно показывать только как результат принятой
модели, а не как безусловный количественный прогноз для реального контакта.

Параметры собственной релаксации и декогеренции КТ в сценарии задаются
феноменологически и не перенастраиваются независимо для каждого зазора. Связь
с диссипативной МНЧ может менять эффективный гибридный отклик внутри
сопряжённых уравнений, но модель не добавляет отдельную эмпирическую,
зависящую от расстояния скорость quenching/Purcell сверх этого отклика.
Поэтому \(\Gamma_{\rm eff}\), пороги и практические рекомендации нельзя
представлять как полный прогноз всех каналов затухания реального близкого
контакта.

## 4. Геометрические каналы

Пусть главная ось сфероида направлена вдоль \(z\). Для полноценного ответа на
вопрос о положении КТ и направлении поля нужно сохранить все пять
неэквивалентных скалярных каналов.

| `channel_id` | Положение КТ | Направление \(\mathbf E\) | Поле относительно локальной поверхности |
|---|---|---|---|
| `axis_long` | у вершины на оси \(z\) | вдоль оси \(z\) | нормальное |
| `axis_trans` | у вершины | поперёк оси | касательное |
| `side_long` | у центра боковой стороны | вдоль оси \(z\) | касательное |
| `side_trans_radial` | у центра боковой стороны | поперёк оси, радиально | нормальное |
| `side_trans_tangential` | у центра боковой стороны | поперёк оси, азимутально | касательное |

Боковое «поперечное поле» нельзя представить одним каналом: радиальное и
тангенциальное направления физически различны. Именно поэтому при первичном
сканировании рисунков 2 и 3 нельзя заранее оставить только ожидаемо выгодную
ориентацию. После объективного ранжирования на этих рисунках последующие
дорогие расчёты можно сузить до лучшего и одного контрольного канала.

Перед разделом результатов настоятельно рекомендуется поместить геометрическую
схему, на которой обозначены \(a\), \(c\), \(r_{\rm QD}\), \(g\), положения
`tip`/`side` и пять направлений \(\mathbf E\). Эта схема не является ещё одной
расчётной парой и не входит в минимальное число вычисляемых графиков.

## 5. Что нужно зафиксировать до первого production-запуска

Все основные рисунки должны быть рассчитаны с одним замороженным набором
параметров. Значения по умолчанию программ пригодны для проверки интерфейса,
но не становятся автоматически параметрами конкретной коллоидной КТ.

| Группа | Что фиксируется | Почему это существенно |
|---|---|---|
| МНЧ | \(c\), \(a\), источник табличных оптических констант золота | Определяет продольный и поперечный LSPR |
| Среда и КТ | \(\varepsilon_m\), \(\varepsilon_{\rm QD}\), \(r_{\rm QD}\), дипольный момент \(d\) и соглашение о его локально-полевой поправке | Определяет амплитуду возбуждения и связь КТ--МНЧ |
| Экситон | \(E_X=\hbar\omega_0\), \(\hbar\gamma_1\), \(\hbar\Gamma_2\) | Определяет положение линии, релаксацию населённости и декогеренцию |
| Импульс | гауссова форма, \(\tau_I=20\) fs как FWHM интенсивности, несущая энергия \(E_L\), момент чтения | Нужен единый перевод флюенса в пиковую интенсивность и единое определение \(P_{\rm exc}\) |
| Материальный fit | общее окно fit, \(N=1\), \(N_{\rm multi}=9\) как начальный production-выбор, веса и допуски | Исключает подгонку представления золота отдельно под каждый рисунок |
| Пространственная сетка | положительные \(g\), `spatial_order_max`, критерий сходимости | Нужна честная граница DD/FQS и надёжность FQS в ближнем поле |
| Спектральная сетка | общее окно вокруг \(E_X\), число точек, фиксированное окно отслеживания пика | Исключает смену определения резонанса между конфигурациями |
| Нелинейная метрика | \(\eta\), диапазон и сетка \(\mathcal F\) | Определяет сравнимый порог на первой возрастающей Rabi-ветви |
| Допуск DD | например, \(\delta_{\rm spec}\le 0.10\), выбранный **до** анализа | Не позволяет подобрать границу применимости по желаемому результату |

Число \(N_{\rm multi}=9\) следует считать принятым только после рисунка 1 и
прохождения сохранённых критериев качества. Если девять полюсов не достигают
заранее заданной точности на нужном окне, следует увеличить
\(N_{\rm multi}\), заново получить все временные результаты и честно изменить
обозначение ветви во всех рисунках.

Ниже приведён шаблон переменных PowerShell. Значения в `[[...]]` должны быть
заменены на экспериментально или библиографически обоснованные значения **до
запуска**. Пока они не заменены, блок намеренно не является готовой командой.

```powershell
$python = ".\.venv\Scripts\python.exe"
$resultDir = "results/article_minimal"
New-Item -ItemType Directory -Force -Path $resultDir | Out-Null

$cNm = [[semi-major axis, nm]]
$aNm = [[semi-minor axis, nm]]
$qdRadiusNm = [[QD radius, nm]]
$epsM = [[host relative permittivity]]
$epsQD = [[QD relative permittivity]]
$dDebye = [[exciton transition dipole, D]]
$omega0EV = [[exciton energy, eV]]
$gammaPopulationMeV = [[population decay energy, meV]]
$gamma2CoherenceMeV = [[coherence decay energy, meV]]
$qdDipoleConvention = "effective_external"

$pulseTauFs = 20.0
$pulseTauKind = "fwhm_intensity"
$carrierEV = $omega0EV
$targetPopulation = 0.5
$populationPostFs = [[one common population read-time endpoint, fs]]
$workPostFs = [[fixed endpoint for the Fourier/work calculation, fs]]

$fitMinEV = 0.8
$fitMaxEV = 3.0
$multiModes = 9
$spatialOrder = 80
$maxModalRelativeError = 0.08
$ddTolerance = 0.10

$spectrumMinEV = [[lower bound around the QD feature, eV]]
$spectrumMaxEV = [[upper bound around the QD feature, eV]]
$featureHalfWindowEV = [[pre-registered tracking half-window, eV]]
$spectrumPoints = 2001

# Более узкое окно, которое обязано лежать внутри Fourier-поддержки 20-fs импульса
$workSpectrumMinEV = [[work-spectrum lower bound, eV]]
$workSpectrumMaxEV = [[work-spectrum upper bound, eV]]
$workSpectrumPoints = 1001

$fluenceMin = [[lower fluence, J/cm^2]]
$fluenceMax = [[upper fluence, J/cm^2]]
$fluencePoints = 65

$gapsNm = @(1.0, 2.0, 3.0, 5.0, 10.0, 20.0, 50.0)
$channels = @(
    "axis_long",
    "axis_trans",
    "side_long",
    "side_trans_radial",
    "side_trans_tangential"
)
```

`populationPostFs` следует выбрать по предварительным auto-window
диагностикам так, чтобы один и тот же физический момент чтения был приемлем для
DD/FQS, всех пяти каналов и обеих материальных реализаций. В production-запусках
рисунков 3 и 5 он задаётся явно: иначе два calculator-а могут независимо
выбрать разные автоматические окна. `workPostFs` относится к другому
наблюдаемому и выбирается по сходимости преобразования Фурье и работы; ему не
требуется совпадать с моментом чтения населённости.

Все calculator-ы защищают существующий NPZ от случайной перезаписи. Команды
ниже намеренно не содержат `--overwrite`; добавлять его следует только при
осознанном полном пересчёте соответствующего артефакта.

Сетка зазоров должна покрывать три области: ближнее поле, переход DD к FQS и
асимптотическое согласие на больших расстояниях. Если к последней точке
\(\delta_{\rm spec}\) ещё не остаётся ниже допуска, верхнюю границу нужно
увеличить. Если изменение наиболее быстро между соседними точками, сетку там
нужно уплотнить до окончательного production-запуска.

## 6. Причинная схема всей последовательности

```text
Рис. 1: direct -> N=1 / Nmulti
        проверка материальной дисперсии
                    |
                    v
Рис. 2: DD -> FQS при одном материале
        эффект МНЧ, сдвиг, ширина, граница DD
                    |
                    v
Рис. 3: нелинейный порог и delta_F для DD/FQS
        выбор лучшего канала, контрольного канала и рабочего зазора;
        граница DD для короткоимпульсного порога
                    |
                    v
Рис. 4: direct -> N=1 / Nmulti в выбранной FQS-геометрии
        перенос ошибки материала в экситонный спектр
                    |
                    v
Рис. 5: N=1 / Nmulti для P_exc(F)
        нелинейное следствие и объективный выбор трёх флюенсов
                    |
                    v
Рис. 6: N=1 / Nmulti для sigma_QS,work(E; F_j)
        Shah-подобный оптический результат и практическая интерпретация
```

Такая последовательность избегает двух логических ошибок. Во-первых,
пространственная ошибка DD не смешивается с ошибкой fit материала. Во-вторых,
геометрия и флюенсы для последних рисунков выбираются по предшествующим
метрикам, а не по наиболее эффектному виду уже рассчитанного оптического
спектра.

## 7. Рисунок 1 — проверка дисперсионной модели МНЧ

### Физическая задача

До обсуждения КТ нужно проверить сам входной отклик МНЧ. Рассчитываются
продольная и поперечная поляризуемости сфероида в трёх представлениях:

- `direct` — интерполированные табличные оптические данные в принятой
  локально-квазистатической формуле;
- \(N=1\) — однополюсный причинный fit;
- \(N=N_{\rm multi}\) — многополюсный причинный fit.

Комплексная \(alpha(E)\) показывает силу и фазу индуцированного диполя; максимум
\(\operatorname{Im}\alpha\) задаёт операционное положение LSPR. Одновременное
сравнение \(\alpha^{-1}(E)\) делает различия положения полюса и затухания
видимыми и не позволяет области с большой \(|\alpha|\) единолично определять
оценку качества fit.

Этот рисунок не содержит КТ и ничего не говорит о DD/FQS. Его роль — дать
независимое основание использовать многополюсную причинную реализацию в
последующих временных расчётах.

### Расчёт и построение

```powershell
$fig01Data = Join-Path $resultDir "fig01_material_dispersion.npz"
$fig01Plot = Join-Path $resultDir "fig01_material_dispersion.png"

& $python -m article_observables.qd_mnp_calculate_material_dispersion_comparison `
    --c-nm $cNm --a-nm $aNm --eps-m $epsM `
    --energy-min-ev $fitMinEV --energy-max-ev $fitMaxEV --energy-points 801 `
    --fit-min-ev $fitMinEV --fit-max-ev $fitMaxEV `
    --one-modes 1 --multi-modes $multiModes `
    --max-normalized-rms 0.025 `
    --max-pointwise-relative-error 0.05 `
    --output $fig01Data

& $python -m article_observables.qd_mnp_plot_material_dispersion_comparison `
    $fig01Data --output $fig01Plot
```

При повторном намеренном пересчёте существующего NPZ нужно явно добавить
`--overwrite`. Изменение оформления выполняется только второй командой.

### Что проверить до перехода к рисунку 2

1. Многополюсная ветвь проходит критерии точности, пассивности и линейной
   устойчивости в обеих ориентациях.
2. Сохранены сдвиги LSPR, FWHM и нормированные ошибки относительно `direct`.
3. Однополюсная ветвь может не пройти критерий точности — это и есть предмет
   сравнения, — но она не должна быть непассивной или неустойчивой.
4. Различие \(N=1\) и \(N=N_{\rm multi}\) обсуждается отдельно для
   продольного и поперечного отклика.

### Каркас текста результатов

> На рисунке 1 сравниваются табличный локально-квазистатический отклик золотого
> сфероида и его причинные реализации с одним и \(N_{\rm multi}=[[...]]\)
> полюсами. Для продольной ориентации однополюсная модель смещает максимум
> \(\operatorname{Im}\alpha\) на \([[...]]\) meV и изменяет FWHM на
> \([[...]]\) %, тогда как многополюсная модель даёт соответственно
> \([[...]]\) meV и \([[...]]\) %. Для поперечной ориентации ошибки равны
> \([[...]]\). Нормированная RMS-ошибка многополюсного fit составляет
> \([[...]]\) и удовлетворяет заранее заданному допуску. Поэтому далее именно
> эта реализация используется как причинное временное представление материала;
> \(N=1\) сохраняется как диагностическая упрощённая модель.

Допустимый вывод: многополюсная реализация точнее **воспроизводит выбранные
табличные данные в указанном окне**. Недопустимый вывод: она является «точным
откликом реального золота» при любых размерах, поверхностях и частотах.

## 8. Рисунок 2 — пространственное уточнение DD -> FQS

### Почему четыре метрики должны идти одним блоком

Руководителем поставлены сразу четыре связанные задачи: эффект МНЧ, сдвиг,
уширение и расстояние совпадения двух пространственных моделей. Все они
извлекаются из одного общего КТ-селективного спектра

\[
S(M,q,g,E),
\]

где \(M\in\{\mathrm{DD},\mathrm{FQS}\}\), \(q\) — один из пяти каналов, а
\(g\) — поверхностный зазор. Поэтому физически и численно правильнее сначала
один раз сохранить полный четырёхмерный массив, а затем без нового решения
получить четыре согласованные панели.

Для минимального production-расчёта используется
`--spectral-observable linear_qd_response`:

\[
S(E,g)=\left|\frac{p_{\rm QD}(E,g)}{E_{\rm inc}(E)}\right|^2.
\]

Это слабополевой монохроматический показатель, а не конечная населённость
после 20-fs импульса. Он выбран для экономичного полного сканирования пяти
каналов, всех зазоров и плотной энергетической сетки. Нелинейный
короткоимпульсный вывод затем независимо проверяется рисунком 3.

В этом частотном DD/FQS-блоке обе ветви используют один и тот же `direct`
табличный отклик материала. Поэтому отличие кривых изолирует именно
пространственное дипольное усечение и не загрязняется ошибкой \(N=1\) против
\(N=N_{\rm multi}\).

Следовательно, переданные в master-команду параметры
`--material-fit-modes` и допуски fit сохраняют единообразный сценарий и нужны
для альтернативного импульсного режима, но при выбранном здесь
`linear_qd_response` не являются источником изображённых DD/FQS-спектров: для
них используется `direct`-ветвь.

### Определения четырёх панелей

Панель 2a:

\[
G_{\rm exc}^{\rm opt}(g)=
\frac{\max_{E\in W}S(E,g)}{\max_{E\in W}S_0(E)}.
\]

Здесь \(S_0\) — спектр изолированной КТ. \(G_{\rm exc}>1\) означает усиление
максимально достижимого слабополевого резонансного отклика при возможности
перестроить лазер; \(G_{\rm exc}<1\) — подавление. Это не равно уменьшению
нелинейного порога, поэтому рисунок 2a не заменяет рисунок 3.

Панель 2b:

\[
\frac{\Delta E_{\rm res}(g)}{\Gamma_0}=
\frac{E_{\rm res}(g)-E_{\rm res,0}}{\Gamma_0}.
\]

\(E_{\rm res}\) — максимум, связанный с линией КТ и отслеживаемый внутри
заранее фиксированного окна \(W\); \(E_{\rm res,0}\) и \(\Gamma_0\) извлекаются
для изолированной КТ тем же алгоритмом. Помимо нормированной величины в NPZ
сохраняется абсолютный сдвиг, который нужно перевести в meV для практической
рекомендации по энергии лазера.

Панель 2c:

\[
\frac{\Gamma_{\rm eff}(g)}{\Gamma_0}.
\]

\(\Gamma_{\rm eff}\) — ширина связной области на половине prominence
отслеживаемого пика. Это операционная ширина спектральной особенности, а не
изменение времени жизни и не автоматически Purcell-уширение. Если появляется
сравнимый второй пик, точка получает статус `split_or_ambiguous`; ей нельзя
приписывать единственную FWHM.

Панель 2d:

\[
\delta_{\rm spec}(g)=
\left[
\frac{\int_W\left(S_{\rm DD}-S_{\rm FQS}\right)^2\,dE}
     {\int_W S_{\rm FQS}^2\,dE}
\right]^{1/2}.
\]

Кривые не нормируются каждая отдельно, поэтому метрика чувствительна и к
форме, и к амплитуде. Граница применимости DD определяется как **первый зазор,
после которого допуск выполняется во всех последующих, более далёких точках**,
а не как первое случайное пересечение линии допуска.

### Один основной расчёт

```powershell
$fig02Master = Join-Path $resultDir "fig02_master_excitation_gain_gap.npz"

& $python -m article_observables.qd_mnp_calculate_excitation_gain_gap `
    --preset publication `
    --gaps-nm $gapsNm --channels $channels `
    --c-nm $cNm --a-nm $aNm --qd-radius-nm $qdRadiusNm `
    --eps-m $epsM --eps-qd $epsQD `
    --d-debye $dDebye --omega0-ev $omega0EV `
    --gamma-population-mev $gammaPopulationMeV `
    --gamma2-coherence-mev $gamma2CoherenceMeV `
    --qd-dipole-convention $qdDipoleConvention `
    --spatial-order-max $spatialOrder `
    --material-fit-modes $multiModes `
    --fit-min-ev $fitMinEV --fit-max-ev $fitMaxEV `
    --max-modal-relative-error $maxModalRelativeError `
    --energy-min-ev $spectrumMinEV --energy-max-ev $spectrumMaxEV `
    --energy-points $spectrumPoints `
    --feature-center-ev $omega0EV `
    --feature-half-window-ev $featureHalfWindowEV `
    --spectral-observable linear_qd_response `
    --dd-tolerance $ddTolerance `
    --output $fig02Master
```

### Три производных NPZ без повторного физического расчёта

```powershell
$fig02bData = Join-Path $resultDir "fig02b_resonance_shift_gap.npz"
$fig02cData = Join-Path $resultDir "fig02c_spectral_width_gap.npz"
$fig02dData = Join-Path $resultDir "fig02d_model_discrepancy_gap.npz"

& $python -m article_observables.qd_mnp_calculate_resonance_shift_gap `
    --source-artifact $fig02Master --output $fig02bData

& $python -m article_observables.qd_mnp_calculate_spectral_width_gap `
    --source-artifact $fig02Master --output $fig02cData

& $python -m article_observables.qd_mnp_calculate_model_discrepancy_gap `
    --source-artifact $fig02Master --output $fig02dData
```

При таком использовании `--source-artifact` физические параметры, сетка и
окно особенности берутся из master-NPZ. Нельзя пытаться «исправить» их новыми
аргументами производных calculator-ов; другой физический сценарий требует
нового master-расчёта. Чисто графическое окно можно менять средствами
plotter-а, если сохранённый диапазон данных это позволяет.

### Построение четырёх панелей

```powershell
$fig02aPlot = Join-Path $resultDir "fig02a_excitation_gain_gap.png"
$fig02bPlot = Join-Path $resultDir "fig02b_resonance_shift_gap.png"
$fig02cPlot = Join-Path $resultDir "fig02c_spectral_width_gap.png"
$fig02dPlot = Join-Path $resultDir "fig02d_model_discrepancy_gap.png"

& $python -m article_observables.qd_mnp_plot_excitation_gain_gap `
    $fig02Master --gain-kind optimized --output $fig02aPlot

& $python -m article_observables.qd_mnp_plot_resonance_shift_gap `
    $fig02bData --output $fig02bPlot

& $python -m article_observables.qd_mnp_plot_spectral_width_gap `
    $fig02cData --output $fig02cPlot

& $python -m article_observables.qd_mnp_plot_model_discrepancy_gap `
    $fig02dData --dd-tolerance $ddTolerance --output $fig02dPlot
```

У gap-plotter-ов нет флага `--allow-unconverged`, и сам факт успешного
построения не заменяет аудит всех сертификатов. Production-master нужно
получать с `--preset publication` и политиками `raise`; нельзя ослаблять их до
`warn`/`ignore` ради рисунка. Для тех plotter-ов, где
`--allow-unconverged` существует, этот флаг в статье также не используется.

### Что проверить

1. Энергетический шаг разрешает \(\Gamma_0\) с заранее заданным запасом.
2. FQS сошёлся по пространственному порядку во всех пяти каналах и на самом
   малом зазоре.
3. Отслеживается именно КТ-связанная особенность, а не далёкий широкий LSPR.
4. Статусы `split_or_ambiguous`, `unresolved` и краевые максимумы не скрыты.
5. Верхний конец сетки достаточен для устойчивого вывода о границе DD.

### Каркас текста результатов

> Рисунок 2a показывает, что влияние МНЧ не имеет универсального знака, а
> определяется положением КТ и направлением электрического поля. В FQS-модели
> максимальное усиление \(G_{\rm exc}=[[...]]\) достигается для канала
> `[[...]]` при \(g=[[...]]\) nm, тогда как канал `[[...]]` даёт
> подавление/меньшее усиление \([[...]]\). Различие DD и FQS максимально в
> области \([[...]]\), что связывается с недипольной пространственной
> неоднородностью поля у поверхности сфероида.
>
> Как видно из рисунка 2b, та же связь смещает КТ-селективный максимум на
> \([[...]]\) meV (\([[...]]\Gamma_0\)); следовательно, практическая
> рекомендация должна включать не только ориентацию, но и энергию лазера
> \(E_L\approx[[...]]\) eV. Рисунок 2c показывает изменение операционной
> ширины до \([[...]]\Gamma_0\). Эта величина характеризует рабочее
> спектральное окно, но не интерпретируется как отдельно рассчитанное изменение
> времени жизни экситона.
>
> Количественная метрика рисунка 2d показывает, что для канала `[[...]]`
> точечно-дипольное описание остаётся вне допуска \([[...]]\) вплоть до
> \(g=[[...]]\) nm. Начиная с \(g_{\rm DD}=[[...]]\) nm и для всех больших
> рассчитанных зазоров \(\delta_{\rm spec}\le[[...]]\). Поэтому далее DD
> можно использовать как экономичную модель только в указанной дальней
> области, тогда как ближнеполевые рекомендации основываются на FQS.

## 9. Рисунок 3 — нелинейный порог, граница DD и выбор конфигурации

### Физическая задача

Слабополевой gain не отвечает напрямую, какой короткий импульс нужен для
заданного возбуждения. Поэтому решаются временные DD- и FQS-модели и
определяется порог на первой возрастающей ветви до первого Rabi-максимума:

\[
\mathcal F_\eta=
\inf\{\mathcal F:\rho_{ee}(t_{\rm read};\mathcal F)\ge\eta\}.
\]

Основная метрика

\[
T_\eta(g)=\frac{\mathcal F_\eta(g)}{\mathcal F_{\eta,0}}
\]

сравнивает гибрид с изолированной КТ. \(T_\eta<1\) означает, что для той же
целевой населённости требуется меньший падающий флюенс. При одинаковой
гауссовой форме и одинаковой FWHM интенсивности \(\tau_I\) отношение пороговых
пиковых интенсивностей совпадает с отношением пороговых флюенсов. Для
используемого вещественного гауссова поля точное соотношение имеет вид

\[
I_{\rm peak}=\frac{2\sqrt{\ln 2}}{\sqrt{\pi}}
\frac{\mathcal F}
{\tau_I\left[1+\exp\!\left(-\omega_L^2\sigma_t^2\right)\right]},
\qquad
\sigma_t=\frac{\tau_I}{2\sqrt{\ln 2}}.
\]

Для многопериодного оптического импульса экспоненциальная поправка практически
исчезает. В численных результатах всё равно следует использовать сохранённое
calculator-ом точное значение `peak_intensity_w_cm2`, а не пересчитывать его
вручную по приближённой формуле.

Панель 3b использует абсолютное относительное расхождение порогов

\[
\delta_{\mathcal F}(g)=
\left|
\frac{\mathcal F_{\eta,\rm DD}(g)-
      \mathcal F_{\eta,\rm FQS}(g)}
     {\mathcal F_{\eta,\rm FQS}(g)}
\right|.
\]

Это нелинейный аналог \(\delta_{\rm spec}\) рисунка 2d. Первая метрика задаёт
границу DD для формы слабополевого спектра, а вторая — для центрального
короткоимпульсного порога. Они не обязаны давать одно и то же расстояние.

Во временном DD/FQS-сравнении обе ветви используют одну и ту же проверенную
многополюсную Lorentz/ADE-реализацию из рисунка 1. Таким образом, различие DD и
FQS снова имеет пространственное происхождение.

### Важное условие интерпретации частоты

Текущий скрипт использует одну общую несущую энергию \(E_L\) для всех каналов
и зазоров. Поэтому основной рисунок отвечает на практически ясный вопрос:
«какой порог имеет каждая конфигурация при одном заранее выбранном лазере», а
не на вопрос о глобально минимальном пороге после независимой оптимизации
частоты каждой точки.

Сдвиг, найденный на рисунке 2b, является выходом и рекомендацией для будущей
настройки лазера; он не должен задним числом изменять уже замороженное
`carrierEV` основного рисунка 3. Рисунок 2a, напротив, показывает
оптимизируемый по энергии слабополевой gain. Это различие двух постановок нужно
явно проговорить в подписи к рисунку.

Минимальный вариант фиксирует \(E_L=E_X\) либо другую экспериментально
заданную энергию **до** запуска. Если рисунок 2b показывает сдвиги, сравнимые с
шириной линии, для выбранного лучшего и контрольного случаев следует сделать
дополнительную проверку при их \(E_{\rm res}\). Без такой проверки итоговый
вывод нужно формулировать как условный относительно указанной \(E_L\).
Такой retuned-laser расчёт является заранее объявленным sensitivity-run и не
заменяет fixed-laser сравнение основного рисунка.

### Расчёт, производная метрика и построение двух панелей

```powershell
$fig03Data = Join-Path $resultDir "fig03_master_threshold_fluence_gap.npz"
$fig03aPlot = Join-Path $resultDir "fig03a_threshold_fluence_gap.png"
$fig03bData = Join-Path $resultDir "fig03b_threshold_discrepancy_gap.npz"
$fig03bPlot = Join-Path $resultDir "fig03b_threshold_discrepancy_gap.png"

& $python -m article_observables.qd_mnp_calculate_threshold_fluence_gap `
    --preset publication `
    --gaps-nm $gapsNm --channels $channels `
    --c-nm $cNm --a-nm $aNm --qd-radius-nm $qdRadiusNm `
    --eps-m $epsM --eps-qd $epsQD `
    --d-debye $dDebye --omega0-ev $omega0EV `
    --gamma-population-mev $gammaPopulationMeV `
    --gamma2-coherence-mev $gamma2CoherenceMeV `
    --qd-dipole-convention $qdDipoleConvention `
    --spatial-order-max $spatialOrder `
    --material-fit-modes $multiModes `
    --fit-min-ev $fitMinEV --fit-max-ev $fitMaxEV `
    --max-modal-relative-error $maxModalRelativeError `
    --pulse-tau-fs $pulseTauFs --pulse-tau-kind $pulseTauKind `
    --post-fs $populationPostFs `
    --carrier-energy-ev $carrierEV `
    --fluence-min-j-cm2 $fluenceMin `
    --fluence-max-j-cm2 $fluenceMax `
    --fluence-points $fluencePoints `
    --target-population $targetPopulation `
    --dd-tolerance $ddTolerance `
    --output $fig03Data

& $python -m article_observables.qd_mnp_calculate_model_discrepancy_gap `
    --source-artifact $fig03Data --output $fig03bData

& $python -m article_observables.qd_mnp_plot_threshold_fluence_gap `
    $fig03Data --target-population $targetPopulation --output $fig03aPlot

& $python -m article_observables.qd_mnp_plot_model_discrepancy_gap `
    $fig03bData --dd-tolerance $ddTolerance --output $fig03bPlot
```

### Правило выбора рабочего сценария для рисунков 4--6

Выбор нужно записать до просмотра последующих графиков.

1. Исключить точки с непройденной сходимостью, цензурированным порогом или
   недостижением \(\eta\) на первой возрастающей ветви.
2. Не использовать зазор меньше заранее принятого физически допустимого
   \(g_{\min}\).
3. Выбрать `bestChannel` и `selectedGapNm` по минимальному FQS-значению
   \(T_\eta\), учитывая результат рисунка 2. Выход DD за допуск не исключает
   ближнюю точку: напротив, это означает, что рекомендацию для неё нужно делать
   по FQS. Исключаются только точки вне физической или численной области
   применимости самой FQS/local-QS постановки.
4. Выбрать `contrastChannel` не просто как произвольную худшую кривую, а как
   физический контроль, меняющий по возможности один фактор: при том же
   положении КТ — ортогональное поле, либо при сопоставимом направлении поля —
   положение `tip` против `side`.
5. После выбора записать абсолютные \(\mathcal F_\eta\), \(I_{\rm peak,\eta}\),
   \(E_L\), \(\delta_{\mathcal F}\) и различие DD/FQS.
6. Считать выбор предварительным до проверки многополюсной ветви относительно
   `direct` для всех пяти каналов на рисунке 4.

```powershell
$bestChannel = "[[selected from Fig. 3]]"
$contrastChannel = "[[pre-defined physical control from Fig. 3]]"
$selectedGapNm = [[selected positive gap, nm]]
```

### Что проверить

- порог находится внутри диапазона флюенсов, а не на его границе;
- сработало уточнение корня на первой возрастающей ветви;
- флюенсная сетка прошла midpoint-проверку;
- населённость остаётся физической, хвост интегрирования и момент чтения
  прошли сохранённые проверки;
- отношение порогов не строится для цензурированных точек;
- одинаковость формы, длительности и несущей импульса для всех ветвей не
  нарушена.
- граница из 3b определяется только по разрешённым порогам и требует, чтобы
  допуск сохранялся во всех последующих точках сетки.

### Каркас текста результатов

> В отличие от слабополевой метрики рисунка 2a, рисунок 3a непосредственно
> характеризует требуемое возбуждающее воздействие. При \(E_L=[[...]]\) eV,
> \(\tau_I=20\) fs и \(\eta=[[...]]\) FQS-модель предсказывает минимальное
> отношение \(\mathcal F_\eta/\mathcal F_{\eta,0}=[[...]]\) для канала
> `[[...]]` при \(g=[[...]]\) nm. Это соответствует уменьшению требуемой
> пиковой интенсивности в \([[...]]\) раза. Для контрольного канала `[[...]]`
> отношение равно \([[...]]\), что показывает роль [[положения КТ / ориентации
> поля]]. DD даёт \([[...]]\), тогда как FQS даёт \([[...]]\); поэтому
> точечно-дипольная модель [[достаточна / недостаточна]] для этой ближнеполевой
> рекомендации.
>
> Рисунок 3b количественно показывает, что граница применимости DD по
> нелинейному порогу равна \(g_{\rm DD}^{(\mathcal F)}=[[...]]\) nm при
> допуске \(\delta_{\mathcal F}\le[[...]]\). Она [[совпадает / не совпадает]]
> с линейно-спектральной границей рисунка 2d, поэтому область применимости DD
> для динамической задачи должна определяться по [[более строгой из двух
> метрик / отдельно указанной метрике]].

Именно после этого шага появляется обоснованная, а не предположенная заранее
рекомендация по размещению КТ и направлению \(\mathbf E\).

## 10. Рисунок 4 — перенос ошибки дисперсии в экситонный спектр

### Почему этот рисунок не дублирует рисунок 1

Рисунок 1 проверяет только поляризуемость МНЧ. Он ещё не доказывает, насколько
ошибка \(\alpha(E)\) существенна для интересующего объекта — экситона КТ.
Поэтому в уже выбранной рисунками 2--3 FQS-геометрии рассчитывается

\[
S(E)=\left|\frac{p_{\rm QD}(E)}{E_{\rm inc}(E)}\right|^2
\]

для `direct`, \(N=1\) и \(N=N_{\rm multi}\). Пространственный кернел, зазор,
КТ и поле фиксированы. Разница теперь имеет только материальное происхождение.

Порядок «сначала предварительно выбрать конфигурацию, затем проверить её
материальную модель» исключает выбор геометрии по наиболее зрелищному
расхождению \(N=1\) и \(N=N_{\rm multi}\). Calculator следует запустить для
всех пяти каналов при выбранном зазоре, чтобы проверить, не является ли
ранжирование рисунка 3 артефактом многополюсного fit. На основном графике для
компактности достаточно показать лучший и один контрольный канал; остальные
кривые остаются в самодостаточном NPZ.

### Расчёт и построение

```powershell
$fig04Data = Join-Path $resultDir "fig04_excitation_spectrum_material.npz"
$fig04Plot = Join-Path $resultDir "fig04_excitation_spectrum_material.png"

& $python -m article_observables.qd_mnp_calculate_excitation_spectrum_material_comparison `
    --preset publication `
    --channels $channels `
    --gap-nm $selectedGapNm `
    --c-nm $cNm --a-nm $aNm --qd-radius-nm $qdRadiusNm `
    --eps-m $epsM --eps-qd $epsQD `
    --d-debye $dDebye --omega0-ev $omega0EV `
    --gamma-population-mev $gammaPopulationMeV `
    --gamma2-coherence-mev $gamma2CoherenceMeV `
    --qd-dipole-convention $qdDipoleConvention `
    --energy-min-ev $spectrumMinEV --energy-max-ev $spectrumMaxEV `
    --energy-points $spectrumPoints `
    --feature-center-ev $omega0EV `
    --feature-half-window-ev $featureHalfWindowEV `
    --spatial-order-max $spatialOrder `
    --multi-fit-modes $multiModes `
    --fit-min-ev $fitMinEV --fit-max-ev $fitMaxEV `
    --max-modal-relative-error $maxModalRelativeError `
    --output $fig04Data

& $python -m article_observables.qd_mnp_plot_excitation_spectrum_material_comparison `
    --input $fig04Data --output $fig04Plot `
    --channels $bestChannel $contrastChannel
```

### Что извлечь

- сдвиг пика \(N=1\) и \(N=N_{\rm multi}\) относительно `direct` в meV;
- ошибку амплитуды и нормированную спектральную невязку;
- изменение операционной ширины;
- возможную смену ранжирования среди всех пяти каналов при переходе от
  \(N=N_{\rm multi}\) к `direct`;
- подтверждение, что \(N=N_{\rm multi}\) проходит сохранённые fit- и
  пространственные сертификаты.

Если многополюсная ветвь заметно расходится с `direct` или меняет лидирующий
канал, выбор после рисунка 3 отклоняется: нужно увеличить
\(N_{\rm multi}\), повторить рисунки 1, 3 и 4, а затем только переходить к
рисункам 5--6. Рисунок 2 при `linear_qd_response` пересчитывать не требуется,
поскольку его DD- и FQS-спектры используют общий `direct`-материал.

### Каркас текста результатов

> Хотя рисунок 1 уже показывает ошибку однополюсной поляризуемости, рисунок 4
> демонстрирует её физическое следствие для КТ. В выбранной конфигурации
> `[[...]]`, \(g=[[...]]\) nm модель \(N=1\) смещает КТ-селективный максимум
> относительно `direct` на \([[...]]\) meV и изменяет амплитуду на
> \([[...]]\) %, тогда как \(N=N_{\rm multi}\) оставляет ошибки
> \([[...]]\) meV и \([[...]]\) %. Следовательно, положение рабочей области
> частот и количественная величина усиления чувствительны к полноценному учёту
> дисперсии. Контрольный канал показывает [[тот же / иной]] уровень
> чувствительности, поэтому вывод [[универсален / зависит от ориентации]].

## 11. Рисунок 5 — нелинейное возбуждение как функция флюенса

### Физическая задача

Частотный рисунок 4 остаётся слабополевым. Центральный нелинейный результат для
короткого импульса — конечная населённость

\[
P_{\rm exc}(\mathcal F)=
\rho_{ee}(t_{\rm read};\mathcal F).
\]

Она рассчитывается для изолированной КТ и FQS-гибрида с \(N=1\) и
\(N=N_{\rm multi}\) при полностью одинаковом импульсе, временном окне,
флюенсной сетке и моменте чтения. Временной ветви `direct` здесь нет: табличная
частотная функция сама по себе не задаёт конечную причинную систему ODE/ADE.
Приемлемость \(N=N_{\rm multi}\) относительно `direct` уже должна быть
установлена рисунками 1 и 4.

Этот рисунок отвечает сразу на три вопроса: меняет ли МНЧ вероятность
возбуждения, как изменяется нелинейный порог и приводит ли \(N=1\) к
количественно другому выводу. Он не является DD/FQS-сравнением: обе гибридные
ветви используют FQS.

### Расчёт и построение

Текущий calculator сохраняет все пять каналов для обеих материальных ветвей;
plotter основного рисунка оставляет только заранее выбранные лучший и
контрольный каналы.

```powershell
$fig05Data = Join-Path $resultDir "fig05_excitation_fluence_material.npz"
$fig05Plot = Join-Path $resultDir "fig05_excitation_fluence_material.png"

& $python -m article_observables.qd_mnp_calculate_excitation_fluence_material_comparison `
    --preset publication `
    --gap-nm $selectedGapNm `
    --c-nm $cNm --a-nm $aNm --qd-radius-nm $qdRadiusNm `
    --eps-m $epsM --eps-qd $epsQD `
    --d-debye $dDebye --omega0-ev $omega0EV `
    --gamma-population-mev $gammaPopulationMeV `
    --gamma2-coherence-mev $gamma2CoherenceMeV `
    --qd-dipole-convention $qdDipoleConvention `
    --pulse-energy-ev $carrierEV `
    --pulse-tau-fs $pulseTauFs --pulse-tau-kind $pulseTauKind `
    --post-fs $populationPostFs `
    --fluence-min-j-cm2 $fluenceMin `
    --fluence-max-j-cm2 $fluenceMax `
    --points $fluencePoints --grid-scale sqrt `
    --spatial-order-max $spatialOrder `
    --one-material-fit-modes 1 `
    --material-fit-modes $multiModes `
    --fit-min-ev $fitMinEV --fit-max-ev $fitMaxEV `
    --max-modal-relative-error $maxModalRelativeError `
    --one-fit-accuracy-policy warn `
    --target-population $targetPopulation `
    --output $fig05Data

& $python -m article_observables.qd_mnp_plot_excitation_fluence_material_comparison `
    $fig05Data --output $fig05Plot `
    --channels $bestChannel $contrastChannel `
    --target-population $targetPopulation
```

### Объективный выбор трёх флюенсов для рисунка 6

Числа нужно выбрать из сохранённых данных рисунка 5 **до** расчёта
оптического спектра:

1. \(\mathcal F_{\rm linear}\) — точка, в которой
   \(P_{\rm exc}/\mathcal F\) ещё находится на слабополевом плато и населённость
   мала; критерий отклонения от плато следует записать заранее, например 1--5 %.
2. \(\mathcal F_\eta\) — интерполированный порог \(P_{\rm exc}=\eta\) на
   первой возрастающей ветви для FQS-многополюсной модели лучшего канала.
3. \(\mathcal F_{\rm nonlinear}\) — первая заранее определённая характерная
   нелинейная точка, например первый максимум населённости после порога. Нельзя
   выбирать её позднее по наиболее эффектной форме \(\sigma(E)\).

```powershell
$fluenceLinear = [[selected from Fig. 5, J/cm^2]]
$fluenceThreshold = [[selected F_eta from Fig. 5, J/cm^2]]
$fluenceNonlinear = [[first pre-defined nonlinear point from Fig. 5, J/cm^2]]
```

### Что проверить

- обе ветви используют буквально одну флюенсную сетку и один момент чтения;
- bare-QD-кривая совпадает между ветвями;
- многополюсная ветвь прошла fit, пассивность, устойчивость, positivity, tail,
  population-decay и fluence-grid проверки;
- непройденный критерий точности \(N=1\) отмечается как диагностический
  результат, но не отменяет требований пассивности и устойчивости;
- порог не извлекается из повторной Rabi-ветви.
- для FQS-многополюсной ветви выбранных `bestChannel` и `selectedGapNm`
  \(\mathcal F_\eta\) согласуется с рисунком 3 в пределах совокупной
  погрешности корневого уточнения и интерполяции сохранённой сетки. При
  несогласии нужно проверить дрейф CLI-параметров и момента чтения, затем
  уплотнить флюенсную сетку;
- метаданные геометрии, КТ, материала, импульса и `populationPostFs` в NPZ
  рисунков 3 и 5 буквально совпадают для общего сценария.

### Каркас текста результатов

> Рисунок 5 переводит частотную ошибку рисунка 4 в нелинейную импульсную
> величину. Для лучшего канала многополюсная FQS-модель достигает
> \(P_{\rm exc}=[[...]]\) при \(\mathcal F=[[...]]\) J/cm\(^2\), тогда как
> однополюсная модель предсказывает \([[...]]\). Порог
> \(\mathcal F_\eta\) изменяется с \([[...]]\) до \([[...]]\) J/cm\(^2\),
> то есть ошибка упрощённой дисперсии составляет \([[...]]\) %. Для
> контрольного канала она равна \([[...]]\) %. Сравнение с bare QD показывает,
> что МНЧ [[снижает / повышает]] требуемый флюенс в \([[...]]\) раза в рамках
> зафиксированного 20-fs сценария.

Из этого же рисунка в тексте следует явно назвать три численных флюенса,
которые затем без изменения передаются в рисунок 6.

## 12. Рисунок 6 — Shah-подобный оптический спектр

### Физическая задача

Усиление населённости КТ не тождественно улучшению оптического отклика всего
гибрида. Поэтому завершающий рисунок строится для величины

\[
\sigma_{\rm QS,work}(E;\mathcal F_j)=
\frac{k(E)}{\varepsilon_0}
\operatorname{Im}
\frac{\widetilde\mu_{\rm total}(E;\mathcal F_j)}
     {\varepsilon_m\widetilde E_{\rm inc}(E;\mathcal F_j)},
\qquad
k(E)=\sqrt{\varepsilon_m}\frac{E}{\hbar c_0}.
\]

Для каждого из трёх флюенсов один временной расчёт с широкополосным импульсом
даёт спектр через преобразование Фурье; отдельное решение ODE для каждой
энергии не выполняется. Вместе с гибридом сохраняются отклик bare MNP и

\[
\Delta\sigma(E;\mathcal F_j)=
\sigma_{\rm hybrid}(E;\mathcal F_j)-
\sigma_{\rm bare\,MNP}(E;\mathcal F_j),
\]

что помогает выделить изменение, связанное с присутствием КТ и её связью с
МНЧ.

Это прямой методический мост к формулам спектрального отклика Shah, но не
строгое воспроизведение его геометрии, DDA/CQED-параметризации или
энергетического разложения. Корректное название — «эффективное
квазистатическое сечение работы/потерь» или «Shah-подобный оптический
показатель». Без отдельного разделения поглощения и рассеяния его нельзя
называть чистым сечением поглощения металла. В нелинейном режиме отношение
\(\widetilde\mu/\widetilde E\) также является отношением выхода и входа для
конкретного импульса, а не частотно-локальной линейной восприимчивостью.

### Расчёт и построение

Для минимального основного рисунка используется только лучший канал. Добавлять
контрольный канал следует лишь тогда, когда статья отдельно обсуждает
геометрическую зависимость оптического спектра, уже установленную по
населённости в рисунках 2--5.

```powershell
$fig06Data = Join-Path $resultDir "fig06_work_loss_spectrum_material.npz"
$fig06Plot = Join-Path $resultDir "fig06_work_loss_spectrum_material.png"

& $python -m article_observables.qd_mnp_calculate_work_loss_spectrum_material_comparison `
    --preset publication `
    --channel $bestChannel `
    --gap-nm $selectedGapNm `
    --fluence-j-cm2 $fluenceLinear $fluenceThreshold $fluenceNonlinear `
    --fluence-label linear threshold nonlinear `
    --selection-source-artifact $fig05Data `
    --carrier-energy-ev $carrierEV `
    --pulse-tau-fs $pulseTauFs --pulse-tau-kind $pulseTauKind `
    --post-fs $workPostFs `
    --energy-min-ev $workSpectrumMinEV --energy-max-ev $workSpectrumMaxEV `
    --energy-points $workSpectrumPoints `
    --c-nm $cNm --a-nm $aNm --qd-radius-nm $qdRadiusNm `
    --eps-m $epsM --eps-qd $epsQD `
    --d-debye $dDebye --omega0-ev $omega0EV `
    --gamma-population-mev $gammaPopulationMeV `
    --gamma2-coherence-mev $gamma2CoherenceMeV `
    --qd-dipole-convention $qdDipoleConvention `
    --one-fit-modes 1 --multi-fit-modes $multiModes `
    --fit-window-ev $fitMinEV $fitMaxEV `
    --max-modal-relative-error $maxModalRelativeError `
    --spatial-order-max $spatialOrder `
    --output $fig06Data

& $python -m article_observables.qd_mnp_plot_work_loss_spectrum_material_comparison `
    $fig06Data --channel $bestChannel --output $fig06Plot
```

Параметр `--selection-source-artifact` сохраняет путь и SHA-256 источника, но
не извлекает флюенсы автоматически. Три численных значения нужно явно
передать через `--fluence-j-cm2`; это защищает происхождение выбора, но требует
ручной проверки единиц.

### Что проверить

1. Энергетическое окно лежит внутри фактической Фурье-поддержки импульса.
   Несущая `carrierEV` также лежит строго внутри
   `[workSpectrumMinEV, workSpectrumMaxEV]`.
2. Численное преобразование падающего поля согласуется с аналитическим.
3. Полное и укороченное временные окна дают согласованный спектр и работу.
4. Спектральная сетка проходит midpoint-проверку.
5. Для обеих материальных ветвей выполнены требования пассивности,
   устойчивости, positivity и сходимости хвоста.
6. Обсуждаются как абсолютный спектр гибрида, так и
   \(\Delta\sigma=\sigma_{\rm hybrid}-\sigma_{\rm bare\,MNP}\): большой
   металлический фон не должен скрывать узкую гибридную особенность.
7. `bestChannel`, `selectedGapNm`, КТ, МНЧ, среда, импульс и обе материальные
   ветви совпадают с принятым сценарием рисунка 5; SHA-источник сам по себе не
   проверяет это физическое тождество.

### Каркас текста результатов

> Рисунок 6 показывает, что изменение конечной населённости на рисунке 5
> сопровождается [[усилением / подавлением / сменой знака]] узкой гибридной
> особенности в \(\sigma_{\rm QS,work}\). При
> \(\mathcal F_{\rm linear}=[[...]]\) J/cm\(^2\) величина
> \(\Delta\sigma\) имеет [[...]] форму, при
> \(\mathcal F_\eta=[[...]]\) — [[...]], а в первом нелинейном режиме
> \(\mathcal F_{\rm nonlinear}=[[...]]\) — [[...]]. Однополюсная модель
> смещает особенность на \([[...]]\) meV и изменяет её амплитуду на
> \([[...]]\) %, тогда как многополюсная ветвь согласована с установленной на
> рисунках 1 и 4 точностью. Это подтверждает, что корректный дисперсионный
> отклик нужен не только для внутренней населённости КТ, но и для
> количественного оптического прогноза.

Допустимый практический вывод должен ссылаться одновременно на рисунки 3, 5 и
6: малая пороговая интенсивность полезна только тогда, когда соответствующее
изменение даёт требуемый знак и величину оптического отклика.

## 13. Как из рисунков получить итоговые рекомендации

| Практический вопрос | Основной источник ответа | Контрольный источник | Формат итогового вывода |
|---|---|---|---|
| Усиливает или подавляет МНЧ возбуждение? | рис. 2a, \(G_{\rm exc}(g)\) | рис. 5, \(P_{\rm exc}(\mathcal F)\) | Указать знак эффекта, канал, зазор, энергию и режим поля |
| Снижается ли требуемая интенсивность? | рис. 3a, \(\mathcal F_\eta/\mathcal F_{\eta,0}\) | абсолютные пороги рис. 5 | Указать \(\eta\), \(E_L\), \(\tau_I\), выигрыш в разах и абсолютное \(I_{\rm peak}\) |
| Сдвигается ли резонанс? | рис. 2b | рис. 4 | Указать знак и абсолютный сдвиг в meV, а затем рекомендуемую энергию лазера |
| Уширяется ли резонанс? | рис. 2c | рис. 4 | Указать \(\Gamma_{\rm eff}/\Gamma_0\) и статус особенности; не делать вывода о времени жизни |
| Где DD достаточно? | рис. 2d для спектра и рис. 3b для порога | обе границы совместно | Указать метрику, допуск и первый зазор, после которого он выполнен во всех дальнейших точках; для общей рекомендации взять более строгую применимую границу |
| Где размещать КТ? | совместное ранжирование рис. 2 и 3 | рис. 5 для выбранных случаев | `tip` или `side`, конкретный положительный \(g\), а не абстрактное «на поверхности» |
| Как направлять поле? | пять каналов рис. 2 и 3 | лучший/контрольный каналы рис. 4--5 | Вдоль/поперёк оси и одновременно нормально/касательно к локальной поверхности |
| Нужна ли многополюсная дисперсия? | рис. 1 и 4 | рис. 5 и 6 | Дать ошибки \(N=1\) и \(N_{\rm multi}\) по резонансу, порогу и оптическому показателю |
| Проявляется ли эффект оптически? | рис. 6 | bare-MNP и \(\Delta\sigma\) | Указать знак, амплитуду и спектральное положение изменения, не называя его чистым поглощением |

Финальный абзац раздела результатов можно строить по следующему шаблону:

> В рамках локально-квазистатической дипольной модели КТ оптимальной среди
> рассмотренных конфигураций является `[[channel]]` при
> \(g=[[...]]\) nm и \(E_L=[[...]]\) eV. По сравнению с изолированной КТ она
> [[увеличивает/уменьшает]] резонансный отклик в \([[...]]\) раза и изменяет
> порог \(P_{\rm exc}=\eta\) в \([[...]]\) раза для 20-fs импульса. Поле при
> этом направлено [[вдоль/поперёк]] оси сфероида и [[нормально/касательно]] к
> локальной поверхности. DD воспроизводит FQS в пределах \([[...]]\) для
> спектра при \(g\ge[[...]]\) nm и для нелинейного порога при
> \(g\ge[[...]]\) nm; для рекомендуемого ближнего расположения необходимо
> использовать FQS. Многополюсное описание золота изменяет предсказанные
> энергию резонанса, порог и Shah-подобный оптический показатель на
> \([[...]]\), \([[...]]\) и \([[...]]\), соответственно, поэтому
> однополюсная аппроксимация недостаточна для количественной рекомендации.

## 14. Что сознательно не входит в основной минимум

| Пара программ | Решение | Когда вернуть |
|---|---|---|
| `qd_mnp_calculate_excitation_fluence.py` -> `qd_mnp_plot_excitation_fluence.py` | Убрать из основного текста: производственная FQS-ветвь уже присутствует в сравнении рисунка 5, а пороги — в рисунке 3 | Только для отдельного расширенного обзора всех каналов при одном материальном fit |
| `qd_mnp_calculate_work_loss_fluence.py` -> `qd_mnp_plot_work_loss_fluence.py` | Убрать из строгого минимума: спектры рисунка 6 при объективно выбранных флюенсах дают более содержательную связь с Shah | Вернуть как панель или Supplement, если нужен прямой аналог зависимости Shah при фиксированной энергии от флюенса |
| `qd_mnp_calculate_population_dynamics.py` -> `qd_mnp_plot_population_dynamics.py` | Условное дополнение: не требуется для ранжирования конструкции и является FQS-only | Обязательно вернуть, если текст обсуждает момент формирования эффекта, Rabi-like осцилляции или временной механизм; показать bare QD, лучший и контрольный каналы |
| \(G_{\rm exc}^{*}(g)\) при фиксированной энергии | Не отдельный основной рисунок; master-NPZ рисунка 2 уже содержит нужные данные | Перестроить без solve через `--gain-kind fixed`, если экспериментальный лазер не перестраивается |

Условный временной рисунок \(\rho_{ee}(t)\) нельзя использовать как
доказательство сходимости DD к FQS: существующая пара `population_dynamics`
показывает FQS и bare QD, а не прямое временное сравнение двух пространственных
моделей.

Рисунок 1 при жёстком лимите числа основных иллюстраций можно перенести в
Supplementary Information, но сам расчёт удалять нельзя. В основном тексте
тогда должны остаться рисунок 4 с ветвью `direct` и численные fit-метрики,
иначе утверждение о преимуществе \(N_{\rm multi}\) останется без эталона.

## 15. Обязательные критерии готовности данных к статье

Ни один график не считается production-результатом только потому, что ODE
успешно интегрировалась. Перед вёрсткой нужно пройти следующие проверки.

### Материал

- одна и та же исходная таблица и одно fit-окно во всех артефактах;
- многополюсный fit проходит заданные ошибки для нужных ориентаций;
- причинная реализация пассивна и линейно устойчива;
- \(N=1\) маркируется как диагностическая аппроксимация, а не как Shah-модель.

### Пространство

- \(c>a\) для реализованного prolate-spheroid workflow;
- размер МНЧ удовлетворяет принятому квазистатическому критерию относительно
  длины волны в среде;
- положительный поверхностный зазор и корректное преобразование в \(R\);
- сходимость FQS по `spatial_order_max` во всех показанных точках;
- уплотнение сетки зазора около найденного перехода DD/FQS;
- отсутствие количественных заявлений о физическом контакте вне области
  применимости локальной модели.

### Спектры

- энергетический шаг достаточно мал относительно \(\Gamma_0\);
- окно отслеживания пика задано до анализа и не переключается между каналами;
- краевые, разделённые и неразрешённые пики имеют явный статус;
- ширина называется операционной спектральной шириной.

### Временная динамика

- одна форма и длительность импульса во всех сравнениях;
- параметры релаксации согласованы с физичностью двухуровневой модели, в
  частности полная скорость декогеренции не меньше половины скорости распада
  населённости;
- физичность матрицы плотности/населённости;
- сходимость временного хвоста и контролируемый момент чтения;
- сходимость флюенсной сетки и порога;
- порог берётся только на первой возрастающей ветви;
- перевод в интенсивность сопровождается указанием профиля и \(\tau_I\).

### Shah-подобный спектр

- три флюенса происходят из рисунка 5, а не выбраны по рисунку 6;
- проверена Фурье-поддержка падающего импульса;
- проверена чувствительность к длине временного окна и энергетической сетке;
- `bare_mnp` и \(\Delta\sigma\) показаны вместе с полным гибридным откликом;
- термин «сечение поглощения» без оговорки не используется.

Флаг `--allow-unconverged` допустим только для технического `quick`-preview с
водяным знаком. Он не должен присутствовать ни в одной команде окончательного
расчёта или построения для статьи. У plotter-ов, не имеющих такого флага,
успешное чтение NPZ само по себе не подтверждает сходимость: нужно проверить,
что исходный calculator был запущен с production-политиками `raise` и завершил
все сохранённые acceptance-gates. Единственное заранее объявленное исключение
— критерий **точности** диагностической ветви \(N=1\), для которого допустим
`warn`: её неточность и измеряется. Пассивность, устойчивость, физичность
населённости и сходимость вычислений ослаблять нельзя.

## 16. Краткая последовательность запуска

После заполнения и замораживания параметров порядок действий таков:

1. Запустить calculator и plotter рисунка 1; принять или изменить
   \(N_{\rm multi}\) по заранее заданным критериям.
2. Запустить единственный master-calculator рисунка 2a.
3. Из его NPZ запустить три производных calculator-а рисунков 2b--2d.
4. Построить четыре панели рисунка 2 и определить область, где нужна FQS.
5. Запустить master-calculator рисунка 3a; из того же NPZ без нового solve
   получить \(\delta_{\mathcal F}\) рисунка 3b. Построить обе панели и
   предварительно зафиксировать лучший канал, контрольный канал и рабочий
   положительный зазор.
6. С этим сценарием рассчитать рисунок 4 для всех пяти каналов, вывести
   лучший и контрольный и проверить выбор относительно `direct`. При
   непройденном gate увеличить \(N_{\rm multi}\) и повторить рисунки 1, 3 и 4.
7. После принятия выбора запустить и построить рисунок 5; проверить его порог
   относительно рисунка 3 и зафиксировать
   \(\mathcal F_{\rm linear}\), \(\mathcal F_\eta\) и
   \(\mathcal F_{\rm nonlinear}\).
8. Явно передать эти три числа и SHA-источник в calculator рисунка 6; построить
   итоговый Shah-подобный спектр.
9. Заполнить численные места `[[...]]` в каркасе текста и сформулировать
   рекомендацию только для прошедших все критерии точек.
10. Лишь после основного вывода решать, нужен ли временной рисунок или
    дополнительные панели в Supplement.

Полученная логика раздела результатов имеет вид:

```text
достоверность материала
-> предел точечно-дипольного пространственного приближения
-> нелинейная карта практической эффективности
-> влияние дисперсии на выбранную конструкцию
-> оптическое проявление
-> конкретная рекомендация по g, положению КТ, E_L и направлению поля.
```
